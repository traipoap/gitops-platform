- name: Deploy Lumina Super-Node (NFS)
  hosts: load-balance
  become: true
  vars:
    nfs_mount_point: "/nfs"

  roles:
    - nfs

  handlers:
    - name: Restart NFS
      service: name=nfs-kernel-server state=restarted
