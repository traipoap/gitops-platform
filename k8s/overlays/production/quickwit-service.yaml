apiVersion: v1
kind: Service
metadata:
  name: quickwit-cluster
  namespace: log-management
  labels:
    app: quickwit
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: grpc
  selector:
    app: quickwit
